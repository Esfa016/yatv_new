export enum UserRoles {
    ADMIN = 'admin',
    PRODUCTION_MANAGER = 'pm',
    EDITOR = 'editor',
    DEPARTMENT_USER ='user'
} 

export enum AllowedUserRoles {
  PRODUCTION_MANAGER = 'pm',
  EDITOR = 'editor',
  DEPARTMENT_USER = 'user',
}